# Review package: f0280bb97a4d47401a60fe25a1bf03ec360c6b1a..HEAD

## Commits
885635d feat: add Profil Paroki link to public navbar
ee177b6 feat: add Zod schema for OrganizationMember
40ceedc feat: add service layer for OrganizationMember queries

## Files changed
 components/public/public-navbar.tsx |  1 +
 schemas/organization.schema.ts      | 23 +++++++++++++++++++++++
 services/organization.service.ts    |  7 +++++++
 3 files changed, 31 insertions(+)

## Diff
diff --git a/components/public/public-navbar.tsx b/components/public/public-navbar.tsx
index eccf0bd..dd6e600 100644
--- a/components/public/public-navbar.tsx
+++ b/components/public/public-navbar.tsx
@@ -8,20 +8,21 @@ import { useState } from "react";
 import {
   Sheet,
   SheetContent,
   SheetHeader,
   SheetTitle,
   SheetTrigger,
 } from "@/components/ui/sheet";
 
 const navLinks = [
   { href: "/", label: "Beranda" },
+  { href: "/profil", label: "Profil Paroki" },
   { href: "/jadwal", label: "Jadwal" },
   { href: "/pengumuman", label: "Pengumuman" },
   { href: "/kegiatan", label: "Kegiatan" },
   { href: "/berita", label: "Berita" },
   { href: "/galeri", label: "Galeri" },
   { href: "/sejarah", label: "Sejarah" },
 ];
 
 export function PublicNavbar() {
   const pathname = usePathname();
diff --git a/schemas/organization.schema.ts b/schemas/organization.schema.ts
new file mode 100644
index 0000000..17bb075
--- /dev/null
+++ b/schemas/organization.schema.ts
@@ -0,0 +1,23 @@
+import { z } from "zod";
+
+export const OrganizationMemberSchema = z.object({
+  name: z
+    .string()
+    .min(2, "Nama minimal 2 karakter")
+    .max(100, "Nama maksimal 100 karakter"),
+  position: z
+    .string()
+    .min(2, "Jabatan minimal 2 karakter")
+    .max(100, "Jabatan maksimal 100 karakter"),
+  photo: z
+    .string()
+    .url("URL foto tidak valid")
+    .optional()
+    .or(z.literal("")),
+  orderIndex: z.coerce.number().int().min(0).default(0),
+});
+
+export const OrganizationMemberUpdateSchema = OrganizationMemberSchema.partial();
+
+export type OrganizationMemberInput = z.infer<typeof OrganizationMemberSchema>;
+export type OrganizationMemberUpdate = z.infer<typeof OrganizationMemberUpdateSchema>;
diff --git a/services/organization.service.ts b/services/organization.service.ts
new file mode 100644
index 0000000..eabe700
--- /dev/null
+++ b/services/organization.service.ts
@@ -0,0 +1,7 @@
+import prisma from "@/lib/prisma";
+
+export async function getAllMembers() {
+  return prisma.organizationMember.findMany({
+    orderBy: { orderIndex: "asc" },
+  });
+}
